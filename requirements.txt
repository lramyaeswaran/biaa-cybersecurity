# Pinned exactly. Verified working together on 2026-07-17, Python 3.13.
#
# NOTE: LangChain/LangGraph 1.x. The older workshop apps pin langgraph==0.2.60;
# this project deliberately uses current 1.x, since a fresh `pip install` today
# gets 1.x and teaching a superseded API ages badly. The StateGraph API used here
# (add_node / add_edge / add_conditional_edges / compile / astream) is unchanged
# between the two, so the lab material carries over.
fastapi==0.139.2
uvicorn[standard]==0.51.0
python-multipart==0.0.32
python-dotenv==1.2.2
jinja2==3.1.6
sse-starlette==3.4.5

langchain==1.3.14
langchain-core==1.4.9
langchain-groq==1.1.3
langchain-ollama==1.1.0
# Ollama Cloud is reached through its OpenAI-compatible endpoint, not langchain-ollama.
# That is what gets usable tool calls out of it — see the note at the top of llm.py.
langchain-openai==1.3.5
langgraph==1.2.9

kubernetes==36.0.3
pydantic==2.13.4
